See [ROS wiki](http://wiki.ros.org/ar_track_alvar) for the users document.

To run the tracking using a RealSense camera, we need:
 - The RealSense ROS Wrapper (built from source) or from Ubuntu packages (Package name `realsense2_ros`)
 - The ros-noetic-rgbd-launch package - Publishes RGBD data to a predefined ROS topic. (topic name: `/camera/depth_registered/points`)
 - Printed AR tags of known dimensions, RealSense camera
 - This repository
 
 Launch commands:
 1. roslaunch realsense2_camera rs_rgbd.launch device_type:=d435 enable_depth:=true
 2. roslaunch ar_track_alvar realsense_depth_track_indiv.launch
 3. rviz -> Change frame -> Add point cloud, tf to the window.
 4. Pose gets published in topic: /ar_pose_marker.

